rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Locked by default — replaced by family-scoped rules in Phase 4 (auth) work.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
